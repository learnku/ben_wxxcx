<view class="content">
    <progress 
        percent="{{progressPercent}}" 
        active-mode="{{progressActiveMode}}"
        stroke-width="2px"
        border-radius="0" 
        active="true"></progress>
</view>